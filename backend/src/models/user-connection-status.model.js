const { addConnection, deleteConnection } = require("./src/db");

const createItem = (uuid, connectionId) => {}
const deleteItem = (connectionId) => {}

module.exports = {
    createItem,
    deleteItem
}
